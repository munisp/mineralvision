# MineralVision Platform - Heavy ML Dependencies (optional install)
# Install with: pip install -r requirements-ml.txt
# Required only for WALDO detection training/inference and deep-learning
# pipelines. Excluded from the lean API image.
-r requirements.txt

torch==2.13.0
ultralytics==8.4.116
pytorch-lightning==2.6.5
transformers==5.14.1
rfdetr==1.9.1
supervision==0.30.0
scikit-image==0.26.0

# Optional SAM segmentation backends (api/vision/sam3 — SAM3-ready
# interface; current loadable backend is ultralytics SAM/SAM2 checkpoint
# or SAM3_SERVICE_URL; native sam3 package supported when installed)
# segment-geospatial==1.1.0   # SAM/SAM2 geospatial helpers (optional)
# sam3                        # native SAM3 backend (optional; install from vendor source)

# Optional GeoLibre client-side GIS integration (api/innovations/geolibre).
# ~50MB browser/WASM wheel (MapLibre + DuckDB-WASM + Whitebox tools); the
# module authors .geolibre.json project documents without it and degrades
# honestly (built-in MapLibre fallback / explicit 503) when absent.
# geolibre==2.5.0
