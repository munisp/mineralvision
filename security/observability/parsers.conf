# JSON is preserved as emitted by Caddy/OpenAppSec where present. Docker's
# Fluentd logging driver already supplies service tag and timestamp metadata.
[PARSER]
    Name        json
    Format      json
    Time_Key    time
    Time_Keep   On
