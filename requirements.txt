# MineralVision Platform - Runtime Dependencies (API + core science)
# Pinned for reproducible builds. Heavy ML deps live in requirements-ml.txt,
# test/lint tooling in requirements-dev.txt.
# Versions verified against PyPI (latest stable).

# Core framework
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
python-multipart==0.0.32

# Database
sqlalchemy==2.0.51
alembic==1.19.1
psycopg2-binary==2.9.12

# Authentication / security (replaces python-jose + passlib)
PyJWT==2.13.0
bcrypt==5.0.0

# Data processing
numpy==2.5.1
pandas==2.3.3
scipy==1.18.0
xarray==2026.7.0

# Geospatial
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
rasterio==1.5.1

# Machine learning (lightweight / CPU-friendly)
scikit-learn==1.9.0
optuna==4.9.0
mlflow==3.15.1

# Computer vision (headless)
opencv-python-headless==5.0.0.93
pillow==12.3.0

# Visualization
matplotlib==3.11.1
plotly==6.9.0
pyvista==0.48.4

# API & HTTP
httpx==0.28.1
aiohttp==3.14.3
requests==2.34.2

# Caching & messaging
redis==8.1.0
aiokafka==0.14.0

# Monitoring & observability
prometheus-client==0.26.0
structlog==26.1.0

# Cloud storage (imported directly by lakehouse data_storage)
boto3==1.43.67

# Data formats
pyarrow==25.0.0
deltalake==1.6.2
h5py==3.16.0
laspy==2.7.0

# Utilities
python-dotenv==1.2.2
pyyaml==6.0.3
tenacity==9.1.4

# Geospatial stack (geodb bridge / geotoolkit)
duckdb==1.4.4

# Optional heavy backends — install when the corresponding service is deployed:
#   geoalchemy2 (PostGIS geometry columns; required when DATABASE_URL is postgres)
#   apache-sedona + pyspark (distributed spatial SQL; enables /innovations/geodb/sedona/knn)
#   geoai-py + segment-geospatial + torch (GeoSAM segmentation & deep change detection;
#     enables GPU backends in /innovations/geoai — CPU fallbacks work without them)
geoalchemy2==0.18.1

# Hard requirement for AES-256-GCM encryption of culturally sensitive
# indigenous knowledge (advanced_indigenous). Without it, encryption paths
# raise RuntimeError instead of silently degrading to XOR.
cryptography>=42.0.0
